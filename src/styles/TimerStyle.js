import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  container: {
    alignItems: 'center',
    marginTop: 50,
  },
  timer: {
    fontSize: 72,
    color: '#fff',
    fontWeight: 'bold',
    marginBottom: 40,
  },
  botoes: {
    flexDirection: 'row',
    gap: 20,
  },
});

export default styles